<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier la tâche</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body { background-color: #f4f6f9; }
        .navbar-brand { font-weight: 700; }
        .form-header {
            background: linear-gradient(135deg, #fd7e14, #e35d00);
            color: white;
            border-radius: 12px 12px 0 0;
            padding: 1.5rem 2rem;
        }
    </style>
</head>
<body>

<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/"><i class="bi bi-clipboard2-check me-2"></i>TaskManager</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMenu">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
    <a class="nav-link" href="/rapport">
        <i class="bi bi-file-text me-1"></i>Rapport
    </a>
</li>
                <li class="nav-item"><a class="nav-link" href="/"><i class="bi bi-house me-1"></i>Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="/dashboard"><i class="bi bi-bar-chart me-1"></i>Dashboard</a></li>
            </ul>
        </div>
    </div>
</nav>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">

            <a href="/" class="btn btn-outline-secondary btn-sm mb-3">
                <i class="bi bi-arrow-left me-1"></i>Retour à la liste
            </a>

            <div class="card shadow border-0 rounded-3">

                <div class="form-header">
                    <h5 class="mb-0 fw-bold"><i class="bi bi-pencil-square me-2"></i>Modifier la tâche</h5>
                    <p class="mb-0 mt-1 opacity-75 small">
                        ID #<%= tache.id %> — Statut actuel :
                        <strong><%= tache.statut === 'a faire' ? 'À faire' : 'En cours' %></strong>
                    </p>
                </div>

                <div class="card-body p-4">
                    <form action="/modifier/<%= tache.id %>" method="POST">

                        <div class="mb-3">
                            <label class="form-label fw-semibold">
                                <i class="bi bi-tag me-1 text-warning"></i>Titre *
                            </label>
                            <input type="text" class="form-control" name="titre"
                                   value="<%= tache.titre %>" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">
                                <i class="bi bi-text-paragraph me-1 text-warning"></i>Description
                            </label>
                            <textarea class="form-control" name="description" rows="3"><%= tache.description %></textarea>
                        </div>

                        <div class="row g-3 mb-3">
                            <div class="col-md-6">
                                <label class="form-label fw-semibold">
                                    <i class="bi bi-flag me-1 text-warning"></i>Priorité *
                                </label>
                                <select class="form-select" name="priorite">
                                    <option value="basse"   <%= tache.priorite === 'basse'   ? 'selected' : '' %>>🟢 Basse</option>
                                    <option value="moyenne" <%= tache.priorite === 'moyenne' ? 'selected' : '' %>>🟠 Moyenne</option>
                                    <option value="haute"   <%= tache.priorite === 'haute'   ? 'selected' : '' %>>🔴 Haute</option>
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-semibold">
                                    <i class="bi bi-calendar-event me-1 text-warning"></i>Date limite *
                                </label>
                                <input type="date" class="form-control" name="date_limite"
                                       value="<%= new Date(tache.date_limite).toISOString().split('T')[0] %>" required>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-semibold">
                                <i class="bi bi-person me-1 text-warning"></i>Responsable *
                            </label>
                            <input type="text" class="form-control" name="responsable"
                                   value="<%= tache.responsable %>" required>
                        </div>

                        <div class="alert alert-warning d-flex align-items-center py-2 mb-4">
                            <i class="bi bi-info-circle me-2"></i>
                            <small>Le <strong>statut</strong> se change depuis la liste principale.</small>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning text-white flex-grow-1">
                                <i class="bi bi-floppy me-1"></i>Enregistrer les modifications
                            </button>
                            <a href="/" class="btn btn-outline-secondary">
                                <i class="bi bi-x-lg"></i>
                            </a>
                        </div>

                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
